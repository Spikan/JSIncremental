// REMOVED: This wrapper is no longer needed
// Decimal is now imported directly from break_eternity.js in each file that needs it

// Legacy export for any remaining references during transition
export const Decimal = (globalThis as any).Decimal;
